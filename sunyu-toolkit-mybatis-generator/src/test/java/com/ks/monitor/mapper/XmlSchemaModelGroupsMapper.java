package com.ks.monitor.mapper;

import com.ks.monitor.pojo.XmlSchemaModelGroups;

public interface XmlSchemaModelGroupsMapper {
    int insert(XmlSchemaModelGroups record);

    int insertSelective(XmlSchemaModelGroups record);
}