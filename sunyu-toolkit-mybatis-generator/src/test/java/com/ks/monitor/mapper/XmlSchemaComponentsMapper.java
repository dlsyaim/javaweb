package com.ks.monitor.mapper;

import com.ks.monitor.pojo.XmlSchemaComponents;

public interface XmlSchemaComponentsMapper {
    int insert(XmlSchemaComponents record);

    int insertSelective(XmlSchemaComponents record);
}