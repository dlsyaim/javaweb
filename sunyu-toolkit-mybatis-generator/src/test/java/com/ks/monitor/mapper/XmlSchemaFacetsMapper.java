package com.ks.monitor.mapper;

import com.ks.monitor.pojo.XmlSchemaFacets;

public interface XmlSchemaFacetsMapper {
    int insert(XmlSchemaFacets record);

    int insertSelective(XmlSchemaFacets record);
}