package com.ks.monitor.mapper;

import com.ks.monitor.pojo.XmlSchemaTypes;

public interface XmlSchemaTypesMapper {
    int insert(XmlSchemaTypes record);

    int insertSelective(XmlSchemaTypes record);
}