package com.ks.monitor.mapper;

import com.ks.monitor.pojo.XmlSchemaCollections;

public interface XmlSchemaCollectionsMapper {
    int insert(XmlSchemaCollections record);

    int insertSelective(XmlSchemaCollections record);
}